generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  uid     String @id
  rfid    String
  fname   String
  lname   String
  sid     String
  v_model String
  phone   String
}
